import React from 'react';
import './Navbar.css';

const Navbar = () => (
  <nav className="navbar">
    <div className="logo">Shoe Shine</div>
    <div className="admin-options">Admin</div>
  </nav>
);

export default Navbar;
